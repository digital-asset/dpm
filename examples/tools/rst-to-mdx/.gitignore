# Compiled binary produced by `make build`.
# Anchored with a leading slash so it only ignores the top-level binary,
# not the cmd/rst-to-mdx/ source directory of the same name.
/rst-to-mdx

# Cross-compile output produced by `make release`.
/dist/
